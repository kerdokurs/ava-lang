loc std::sample;

const x: &u8 = 1;

var y = 2.2;
var z = true;
var z = nil;
var w = z;
var i = "testing";
var k = (1);
var m = -1;
var a = 1 + 1;
var c = 1 + 2 - 1;
var e = (1 + 1) * 2;
const ab = a;

fun test(a: u8) -> void {
    if a == 0 {
        Print(a+1);
    } else {
        Print(a);
        a = 5;
        Print(a);
    }

    var n = 0;
    while n < 5 {
        n = n + 1;
    }
}

fun main() -> void {
    test(10);
    test(0);
    Print(a);
}